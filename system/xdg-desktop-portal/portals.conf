# The headless :1 session runs openbox, not GNOME. xdg-desktop-portal defaults to
# the gnome backend, which never answers without a GNOME session -- Qt apps then
# block on portal calls (Settings.ReadAll -> NoReply) and never paint their window.
# Force the gtk backend, which works under a bare WM.
[preferred]
default=gtk
