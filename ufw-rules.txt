Status: active
Logging: on (low)
Default: deny (incoming), allow (outgoing), deny (routed)
New profiles: skip

To                         Action      From
--                         ------      ----
Anywhere on lo             ALLOW IN    Anywhere                  
Anywhere                   ALLOW IN    192.168.0.0/24             # LAN
Anywhere                   ALLOW IN    100.64.0.0/10              # Tailscale CGNAT
Anywhere on tailscale0     ALLOW IN    Anywhere                   # Tailscale interface
41641/udp                  ALLOW IN    Anywhere                   # Tailscale VPN port
Anywhere                   ALLOW IN    172.16.0.0/12              # Docker bridge networks
4500/udp                   ALLOW IN    Anywhere                   # Tailscale VPN port (moved off 41641)
32400/tcp                  ALLOW IN    Anywhere                   # Plex direct remote access (R2 amended 2026-08-22): Plex is shared with non-tailnet users e.g. Ed; without this UFW DROPs the gateway-forwarded SYN and every remote client silently falls back to relay at 1500kbps SD. Gateway must also forward tcp/32400 - neither works alone. See docs/network-architecture.md 4.2
Anywhere (v6) on lo        ALLOW IN    Anywhere (v6)             
Anywhere (v6) on tailscale0 ALLOW IN    Anywhere (v6)              # Tailscale interface
41641/udp (v6)             ALLOW IN    Anywhere (v6)              # Tailscale VPN port
4500/udp (v6)              ALLOW IN    Anywhere (v6)              # Tailscale VPN port (moved off 41641)
32400/tcp (v6)             ALLOW IN    Anywhere (v6)              # Plex direct remote access (R2 amended 2026-08-22): Plex is shared with non-tailnet users e.g. Ed; without this UFW DROPs the gateway-forwarded SYN and every remote client silently falls back to relay at 1500kbps SD. Gateway must also forward tcp/32400 - neither works alone. See docs/network-architecture.md 4.2

